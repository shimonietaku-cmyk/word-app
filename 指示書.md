中学生（中2・中3）の息子のための英単語学習Webアプリを作ってください。
教材は東京書籍『NEW HORIZON English Course 1・2』の巻末「単語・連語のチェック」全1,961語です。
GitHub Pages に置いて、URLを渡すだけで息子のスマホから使えるようにするのが目的です。

# 最重要の制約（絶対に守ること）

1. **完全ローカル動作**。サーバー、ログイン、アカウント、外部APIを一切使わない。
   学習進捗は localStorage にのみ保存する。氏名・メールアドレス等の個人情報は入力させない。
   ネットワーク通信は静的ファイルの初回読み込みのみ。
2. **スマホ縦画面ファースト**。片手・親指だけで完結する操作にする。タップターゲットは最低48px。
3. **他人との比較要素を入れない**（ランキング、対戦、順位表は禁止）。成長は必ず「自分比」で見せる。
4. **ライフ／ハート制を入れない**。間違えて学習が止まる仕組みは作らない。
5. 広告・課金導線を入れない。
6. **応答は16ms以内**。タップ・スワイプ・キー入力に遅延なく反応させる。
   重い再計算をレンダリング経路に置かない、画像を使わない、レイアウトシフトを起こさない。

# 技術スタック

- React 18 + Vite + TypeScript / Tailwind CSS / Recharts（グラフ）
- **ts-fsrs**（`npm i ts-fsrs`）… 間隔反復のスケジューリング。ブラウザで動作する
- vite-plugin-pwa（ホーム画面に追加＋オフライン動作）
- デプロイ：GitHub Pages（GitHub Actions のワークフローも作成し、vite.config.ts の base を設定）
- テスト：Vitest（アルゴリズムと採点のユニットテストのみ）

# データ

## 単語データ `words.json`（作成済み・1,961語）

同じフォルダの `words.json` を `public/data/words.json` に移動して使う。1年 1,115語／2年 846語。

```ts
type WordType = 'word' | 'phrase' | 'expression' | 'form';
type Pos = 'noun'|'verb'|'adjective'|'adverb'|'function'|'number'|'phrase'|'expression'|'form';

interface Word {
  id: string;        // "g2-unit-1-0013"
  grade: 1 | 2;
  unit: string;      // "Unit 1" / "Daily Life Scene 1" / "Let's Read 1" / "Active Grammar 1" / "World Tour 1" / "You Can Do It! 1" / "Sounds and Letters" / "Let's Be Friends!"
  part: number | null;
  section: 'unit'|'dls'|'grammar'|'read'|'sounds'|'world'|'ycdi';
  en: string;        // "look for 〜" のように 〜 [] () を含みうる
  ja: string[];      // 訳の配列
  jaMain: string;    // 4択の選択肢に使う代表訳
  type: WordType;
  pos: Pos;          // 品詞（日本語訳から自動判定。誤選択肢の生成に使う）
  level: 1|2|3;      // 綴りの長さによる難易度（1:4文字以下 2:5-7 3:8以上）
  note: string;      // "meet の過去形、過去分詞" など。無ければ ""
  srcPage: string;   // 出典ページ（デバッグ用。UIでは使わない）
}
```

- データはコードにハードコードせず、必ず `words.json` から読み込むこと。
- **単元(unit)の並び順は `words.json` の登場順が教科書どおり**。文字列ソートすると単元順が壊れるので、範囲選択のUnit一覧は必ず**JSONの出現順**で表示すること。
- `pos` は日本語訳からの自動判定なので完璧ではない。誤選択肢の生成に使うが、候補が足りないときは条件を緩めてフォールバックすること。

## 進捗データ（localStorage、キー `wordclimb.v1`）

```ts
interface CardState {
  stage: 1 | 2 | 3;          // 1:受容(4択) 2:想起(スワイプ) 3:産出(並べ替え/入力)
  fsrs: FSRSCard;            // ts-fsrs のカード状態（due, stability, difficulty, reps 等）
  correct: number;
  wrong: number;
  consecutiveWrong: number;
  fastCorrect: number;       // 2秒以内に正解した回数（stage昇格の判定に使う）
  lastSeen: string;
  leech: boolean;
}

interface Store {
  version: 1;
  cards: Record<string, CardState>;
  streak: { current: number; best: number; lastStudyDate: string; freezes: number; days: string[] };
  settings: {
    dailyNewLimit: number;   // default 20（15〜25の範囲で選択可）
    sessionSize: number;     // default 10（1セッションの問題数）
    scope: { grades: number[]; units: string[] };
    audio: boolean;          // default true
    darkMode: 'auto'|'light'|'dark';
  };
  history: { date: string; answered: number; correct: number; newLearned: number }[];
  testMode: { active: boolean; units: string[]; testDate: string } | null;
}
```

書き込みのたびに保存し、JSONパース失敗時は初期状態に安全にフォールバックすること。

---

# 学習設計（ここが本体。`src/lib/` に純関数で実装しVitestでテスト）

## A. 3段階の動的スキャフォールディング（足場かけ）

いきなりスペルを書かせると中学生は離脱する。負荷を段階的に上げること。
**同じ単語でも、その単語の習熟度に応じて出題形式が変わる。**

| Stage | 名称 | 出題形式 | 昇格条件 |
|---|---|---|---|
| **1** | 受容フェーズ | 英→日の4択 | 4択で**2回連続正解** |
| **2** | 想起フェーズ | 英語カードを見て頭の中で答え、タップで裏返して自己判定（選択肢なし） | **2秒以内の正解が2回**（fastCorrect >= 2） |
| **3** | 産出フェーズ | 日→英。**まず文字パネルの並べ替え**、それに2回成功したらキーボード入力 | — |

- Stage 3 の並べ替えは、正解の綴りの文字をシャッフルし、ダミー文字を2つ加えたパネルをタップして並べる方式。モバイルでの入力負荷を大きく下げられる。
- Stage が下がる条件：どの段階でも不正解が2回続いたら `stage = max(1, stage - 1)` に落とす。

## B. 誤選択肢（デストラクタ）の自動生成 ← 最重要

4択を「当てずっぽう」にさせない。**同じ問題セットの中から、紛らわしい選択肢を選ぶ**。
研究上、選択肢が競合的だと、脳が「なぜ他が違うか」まで想起するため定着が大きく上がる。

`generateDistractors(target, pool)` を実装し、以下の優先順位で3つ選ぶ：

1. **同じ `pos` かつ 同じ `unit`**（＝同じ単元の同じ品詞。最も紛らわしい）
2. **同じ `pos` かつ 同じ `level`**（品詞と難易度が一致）
3. **綴りが似ている**：`en` の編集距離が3以内、または先頭2文字が一致（例 `though / thought / through`, `quiet / quite`）
4. **意味カテゴリが近い**：`jaMain` の末尾1文字が一致（「〜な」形容詞同士、「〜する」動詞同士）
5. 上記で3つ埋まらなければ、同じ `grade` からランダムに補充

**禁止事項**：正解と `jaMain` が完全一致する語を選択肢に入れないこと（`play` のように同じ語が複数エントリある。重複訳は必ず除外する）。
選択肢の並び順は毎回シャッフルすること。

## C. 復習スケジューリング（FSRS）

`ts-fsrs` を使う。自前で間隔テーブルを書かないこと。

```ts
import { fsrs, generatorParameters, Rating, createEmptyCard } from 'ts-fsrs';
const f = fsrs(generatorParameters({ request_retention: 0.90, enable_fuzz: true }));
```

- **目標定着率 `request_retention` は 0.90**。テスト対策と長期定着を両立する水準。
- 設定画面に「復習の多さ」スライダーを置き、0.85（少なめ）／0.90（標準）／0.95（多め）を選べるようにする。

### 評価（Rating）はユーザーに選ばせず、自動で決める

中学生に Again/Hard/Good/Easy の4ボタンを出すと迷って止まる。**正誤と解答時間から自動で決めること。**

| 条件 | Rating |
|---|---|
| 不正解 | `Rating.Again` |
| 正解、かつ解答時間 > 6秒 | `Rating.Hard` |
| 正解、かつ 2〜6秒 | `Rating.Good` |
| 正解、かつ 2秒未満 | `Rating.Easy` |

- 解答時間は「問題が描画された時刻」から「回答が確定した時刻」まで。Stage 3 の入力は打鍵に時間がかかるので、**Stage 3 のみ閾値を 2秒→5秒、6秒→15秒に緩める**。
- 「遅いが正解」を `Again` に落とさないこと。遅い＝忘れかけ、であって間違いではない。

## D. 1日の新規学習制限（挫折防止）

やる気のある初日に100語進めると、数日後に復習が爆発して挫折する。

- **1日に新しく出す単語は最大 `dailyNewLimit`（デフォルト20語）まで。** 上限に達したら、その日はもう新規を出さず復習だけにする。
- 上限に達したときは「今日の新しい単語はここまで。あとは復習でしっかり固めよう」と**肯定的に**表示する。制限を罰のように見せないこと。
- 設定で 15／20／25 から選べる。それ以上は選べないようにする。

## E. セッションの組み立て（1セッション10問・30秒〜1分で終わる）

```
1. scope（選択中の学年・Unit）に含まれ、leech でないカードを対象にする
2. due <= 今日 の復習カードを優先して取る
3. 残り枠を新規カードで埋める（ただし本日の新規消化数が dailyNewLimit 未満のときだけ）
4. 復習だけで sessionSize に達する日は新規ゼロ
5. 出題順はシャッフル（インターリービング）
   ただし「新規学習モード」を選んだときだけは words.json の出現順（＝単元順）を保つ
```

## F. 満点体験で終わらせる（自己効力感）

セッション中に間違えた単語は、**そのセッションの最後に必ずもう一度出す**（全問正解するまで、最大3周）。
結果画面は「**10問中10問正解！**」で終わるようにする。この最終再出題ぶんは FSRS の評価には使わず、統計上も初回の正誤で記録すること（実力を過大評価しないため）。

## G. 難単語（leech）の隔離

`consecutiveWrong >= 3` かつ `wrong >= 8` になったカードは `leech = true` にして通常セッションから外す。
「苦手特訓」画面からのみ、Stage 1（4択）に戻して少量ずつ出す。

## H. 採点の正規化（`src/lib/judge.ts`）

`look for 〜` のような表記が混ざるため、比較前に正規化する：

1. 小文字化 → 2. 前後の空白除去・連続空白を1つに → 3. `~` `〜` と `[...]` `(...)` を中身ごと除去
4. `.` `,` `!` `?` `'` `’` を除去 → 5. 完全一致なら正解
6. **編集距離1以内なら「おしい！」**。正解にはせず、正しい綴りを大きく表示して再入力させる

Vitestのテストケース例：
`"look for ~"` ← `"look for"` は正解 ／ `"I'm"` ← `"im"` は正解 ／
`"beautiful"` ← `"beatiful"` は「おしい」 ／ `"beautiful"` ← `"bad"` は不正解

---

# 音声（Web Speech API）

`speechSynthesis` を使う。**外部APIは使わない。**

## 再生タイミング（方向によって変えること）

| モード | 再生タイミング | 理由 |
|---|---|---|
| Stage 1・2（英→日） | **問題が表示された瞬間に自動再生** | 音と意味の連合を強化する |
| Stage 3（日→英） | **答え合わせの瞬間に自動再生** | 問題表示時に鳴らすと答えを教えてしまう |

どの画面にもスピーカーボタンを置き、手動で聞き直せるようにする。設定でOFFにできること。

## iOS対応（必須・ハマりどころ）

iOS Safari は**ユーザー操作なしの `speechSynthesis.speak()` を無視する**。
そのため「セッションを開始する」ボタンのタップハンドラ内で、
無音の `SpeechSynthesisUtterance('')` を1回 `speak()` して**音声を解錠**しておくこと。
これをやらないと、iPhoneで一切音が鳴らない。

`speechSynthesis.getVoices()` が空配列を返すことがあるので `voiceschanged` イベントを待つ。
英語音声が見つからない環境ではスピーカーボタンを非表示にする。

---

# 画面

## ① ホーム
- 主役は **「今日の10問をはじめる」大ボタン1つ**。他は小さく
- **ストリークをカレンダーで表示**（今月のマス目を、学習した日は塗る）。「🔥 15日連続」を大きく
- 今日の進捗リング（例 20/30問）と「今日の新規：12 / 20語」
- 下部タブ：ホーム / 範囲 / 記録 / 設定

### ストリーク仕様
- その日 sessionSize 以上を回答したら「学習した日」としてカウント
- **週に1回、自動でストリークフリーズを1個付与**（上限2個）。1日抜けてもフリーズが自動消費される
- フリーズが尽きて2日空いたらリセット。**リセット時に責める文言を絶対に出さない**（「またここから」と表示）

## ② 学習セッション
- 上部に進捗バー（3/10）と「やめる」
- **Stage 1（4択）**：英単語を大きく中央に、日本語4択。誤選択肢は上記Bのルールで生成
- **Stage 2（想起・スワイプ）**：英単語のみ表示 → タップで裏返して意味を表示 → 「わかった／あやふや」を左右スワイプまたは2ボタンで自己申告
- **Stage 3（産出）**：日本語（ja全訳を表示）→ 文字パネルの並べ替え、または入力欄
- 正解 → **0.4秒**の軽いフィードバックで自動的に次へ（ボタンを押させない）
- 不正解 → 正解を大きく表示、`note` があれば併記、「わかった」で次へ
- 終了画面：**「10問中10問正解！」**／新しく覚えた語数／昨日より何語増えたか／今日のストリーク

## ③ 範囲
- 学年 → Unit → part の階層チェックボックス（親チェックで子も全選択）
- プリセット：「1年ぜんぶ」「2年ぜんぶ」「苦手な単語だけ」「今週の復習」
- **テストモード**：Unit と実施日を指定すると、FSRSのdueを無視してその範囲を全問出題。残り日数と範囲内の未習熟語数を表示

## ④ 記録
- Unit別 習熟度ヒートマップ（Stage3到達=緑／Stage2=黄／Stage1=橙／未着手=グレー）。タップでその単元を範囲に設定
- 直近30日の学習量グラフ（Recharts）
- 苦手単語トップ20（leechリスト）＋「ここだけ特訓する」ボタン
- **他人との比較・順位は一切出さない**

## ⑤ 親子クイズモード（任意機能）
- 親のスマホに英単語と答えを表示し、子が口頭で答える出題モード
- 親が「正解／おしい」をタップするだけ。10問で終わる
- **成績はメイン進捗に反映するが、FSRSの評価は `Good` 固定にする**（親の判定基準がばらつくため）
- 起動は設定からの明示的な操作のみ。ホームに常設しない（親のチェックが監視のプレッシャーにならないように）

## ⑥ 設定
- 1セッションの問題数（10 / 15 / 20）
- 1日の新規学習上限（15 / 20 / 25）
- 復習の多さ（少なめ0.85 / 標準0.90 / 多め0.95）
- 音声 ON/OFF、ダークモード
- 進捗の書き出し（JSONダウンロード）／読み込み／全消去（確認ダイアログ必須）

---

# デザイン

- アクセント1色＋グレースケール中心。派手にしない
- アニメーションは0.2〜0.3秒。長い演出はテンポを殺す
- システムフォント（`-apple-system, "Hiragino Sans", sans-serif`）
- ダークモード対応

# 進め方

1. まずプロジェクト構成と実装計画を提示して、私の確認を取ってから着手する
2. `src/lib/scheduler.ts`（ts-fsrsのラッパー＋Rating自動判定）、`src/lib/distractors.ts`（誤選択肢生成）、
   `src/lib/judge.ts`（採点正規化）、`src/lib/session.ts`（セッション組み立て）を先に実装し、Vitestを通す
3. その後UIを実装する
4. `npm run dev` で起動確認し、`npm run build` が通ることを確認する
5. README に「ローカル起動」「GitHub Pagesへのデプロイ手順」「words.json の差し替え方」を書く

コード内のコメントとUI文言はすべて日本語で。
私は非エンジニアなので、手順は省略せず具体的に説明してください。
